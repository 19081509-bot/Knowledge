# 安全区（主库）

> 本笔记为所有 AI 的统一安全区说明。**主安全区 = `C:\Users\Administrator\Documents\Codex`**（整个目录已在 Defender 排除）。

## 规则
1. 所有 AI 的脚本/可执行工具先放安全区内的各自子文件夹再使用（如 `Codex\Scripts\`、`Codex\hermes\`）
2. 受控文件夹白名单已含 python/node/pwsh；当前用户 PowerShell = Bypass
3. 防火墙已放行 Codex++/Hermes/python/Tailscale；出站默认允许
4. 若再遇"阻止写文件"：把对应 exe 加进 `ControlledFolderAccessAllowedApplications`

## 已安放的组件
- `Codex\Scripts\win-ocr.py` — Windows 端扫描 PDF → 文本（RapidOCR 本地引擎，中英）
- `Codex\hermes\` — Hermes 物理数据（原 `%LOCALAPPDATA%\hermes` 为 Junction 透明指向此处）
- `Documents\Codex\2026-08-21\jia` — 当前 Codex 会话工作目录

## 历史
- 2026-08-26 曾暂用 `C:\Users\Administrator\SafeZone`（Hermes 首迁），2026-08-27 统一归入主安全区 `Documents\Codex`
