# Mac 侧任务简报 · 微信记录导出清洗（交给 Claude Code CLI）

> 派单人：Windows Hermes（总调度）。执行人：Mac Claude Code CLI（v2.1.247）。
> 时间：2026-08-27。本机网络：Clash 规则模式，系统代理 127.0.0.1:7897 已开，直连 github/硅基流动均通。

## 一、你的角色与边界
你是 Mac 上的 Claude Code CLI，受 Windows Hermes 调度。本任务**只做文件操作、写指引、准备结构**；
**不碰微信加密库、不运行需 GUI 双击的安装、不修改任何微信原始数据**。

## 二、当前目标（分阶段）
### 阶段1（你现在做，纯命令行）
1. 确认知识库已同步到 Mac：`/Users/w/Documents/知识库/微信聊天记录/` 存在且含 `00-说明.md`
2. 读取 `00-说明.md`，完全理解清洗规则（按联系人归档、脱敏、剔占位符、绝不碰加密库）
3. 在 Mac 知识库建好导出/清洗目录（应已存在，确认即可）：
   `/Users/w/Documents/知识库/微信聊天记录/01-原始导出/` 与 `02-清洗后/`
4. 写一份 `Mac安装指引.md` 到 `微信聊天记录/` 目录：
   - 说明 WeChatDataAnalysis 的 macOS dmg 从 GitHub Release 下载地址
   - 安装后如何导出「单个联系人/群」聊天记录为 JSON/TXT
   - 导出文件应放到 `01-原始导出/<联系人名>/Mac-YYYYMMDD.json`
   - 注明：GUI 安装需东哥手动，Claude 不代点

### 阶段2（东哥手动装完 dmg 并导出后，Hermes 再派你）
- 读取 `01-原始导出/<联系人>/` 下所有文件
- 按 `00-说明.md` 规则清洗 → 写 `02-清洗后/<联系人>.md`
- 同一联系人多次导出则合并追加，不覆盖

## 三、关键约束（写进 Mac安装指引.md）
- 只处理微信客户端/WeChatDataAnalysis 的**明文导出**，严禁读取 SQLCipher 加密库
- 导出含他人隐私，清洗落库前必须脱敏（手机/身份证/银行卡打码）
- 不修改原始聊天数据，只产出清洗后 markdown

## 四、完成后回报
把 `Mac安装指引.md` 写好后，回报：文件已建 + 目录确认结果。不要自行执行阶段2（等东哥导出）。
